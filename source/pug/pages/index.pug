extends ../components/base/layout

block variables
  - const lang = "ru"
  - const page = "index"
  - const title = "TOO Asia Petro Service"
  - const description = "TOO Asia Petro Service - поставка буровых растворов, химии для бурения и скважин в Актау, Казахстан"
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = "ru"
  - const hasFooter = true
  - const footerMods = "ru"
  - const openGraf = true
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +hero(lang)
    +index-about(lang)
    +news(lang)
    +advantages(lang)
    +index-contacts(lang)
    +index-form(lang)

block modals
